packages:
  - 'server'
  - 'web'
  - 'packages/*'

# pnpm >=10 blocks dependency postinstall scripts by default.
# esbuild (via Vite) needs its install script for the platform binary.
allowBuilds:
  esbuild: true
