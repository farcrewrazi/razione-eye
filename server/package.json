{
  "name": "@razione-eye/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node src/scripts/dev.ts",
    "seed": "node src/scripts/seed.ts",
    "backup": "node src/scripts/backup.ts",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@razione-eye/shared": "workspace:*",
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.13",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  }
}
